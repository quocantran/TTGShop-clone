mixin link(title,target)
    .link-sale 
        a(href=`/collections/${target === "undefined" ? "all" : target}` class="onsale-link")
            p XEM TẤT CẢ #{title}
            <i class="fa-regular fa-circle-right"></i>